name: automatic-develop-validation
on:
  push:
    branches:
      - develop
jobs:
  check-ls:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
      - run: ls -al